# CoinMatch Backend Environment Variables
# Copy to .env and fill in values. On Railway, set these in the backend service's Variables tab.

# Database (Railway → add Postgres plugin → reference ${{Postgres.DATABASE_URL}})
DATABASE_URL=postgresql://user:pass@host:5432/coinmatch

# Redis (Railway → add Redis plugin → reference ${{Redis.REDIS_URL}})
REDIS_URL=redis://localhost:6379

# Auth — generate a fresh one with:  python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=change-me-to-a-64-char-random-string

# Stripe Connect (stripe.com → Developers → API keys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Claude Vision — AI coin grader (console.anthropic.com → API Keys)
# Without this set, the grader falls back to a heuristic stub.
ANTHROPIC_API_KEY=sk-ant-...
# Optional override; defaults to claude-sonnet-4-6
CLAUDE_MODEL=claude-sonnet-4-6

# Whop (handles Pro Collector $19/mo and Dealer $99/mo subscriptions).
# Set up products in your Whop dashboard, then paste the public checkout URLs
# and the webhook signing secret here. Stripe still handles the coin-sale
# marketplace transactions (Stripe Connect, above) — Whop only does subs.
WHOP_WEBHOOK_SECRET=whsec_...
WHOP_PRO_CHECKOUT_URL=https://whop.com/checkout/...
WHOP_DEALER_CHECKOUT_URL=https://whop.com/checkout/...

# Google OAuth (optional for MVP — skip these if not using Google login)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# App
APP_ENV=production
APP_URL=https://getcoinmatch.com
CORS_ORIGINS=https://getcoinmatch.com
OWNER_EMAIL=jessebecker2021@gmail.com
PORT=8000
