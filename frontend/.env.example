# CoinMatch Frontend Environment Variables
# Copy to .env.local for local dev. On Railway, set in the frontend service's Variables tab.

# Public URL of the backend API (FastAPI on its own Railway service).
# Local:  http://localhost:8000
# Prod:   https://<your-backend-service>.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8000
